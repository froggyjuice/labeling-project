# 프로덕션 환경 변수 예시
# 실제 사용시 .env.production으로 복사하여 실제 값으로 변경하세요

# Django 설정
DJANGO_SECRET_KEY=your-super-secret-key-here-generate-new-one
DJANGO_SETTINGS_MODULE=labeling_project.settings_production

# 데이터베이스 설정 (PostgreSQL 권장)
DB_NAME=labeling_project_prod
DB_USER=labeling_user
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# Redis 설정 (캐시 및 세션 스토리지)
REDIS_URL=redis://127.0.0.1:6379/1

# Google OAuth 설정
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret

# 이메일 설정 (알림용)
EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
ADMIN_EMAIL=admin@your-domain.com

# SSL 인증서 경로 (Let's Encrypt 등)
SSL_CERT_PATH=/etc/letsencrypt/live/your-domain.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/your-domain.com/privkey.pem

# 도메인 설정
DOMAIN_NAME=your-domain.com
ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# Cloudflare 설정 (사용시)
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ZONE_ID=your-zone-id 